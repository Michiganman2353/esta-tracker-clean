# ==================== #
# ESTA Tracker Environment Variables
# ==================== #
# For local development: Copy this file to .env (gitignored)
# For production: Configure in Vercel Dashboard → Settings → Environment Variables
# For CI/CD: Configure in GitHub repository secrets

# ==================== #
# Frontend Configuration (MANDATORY - Required for Build)
# ==================== #
# ⚠️ REQUIRED: This monorepo uses VITE_ prefix EXCLUSIVELY for all frontend environment variables
# REACT_APP_* and unprefixed FIREBASE_* variables are NO LONGER SUPPORTED
# All six variables below are MANDATORY for the application to build and run
# Without these, the build will fail with clear error messages

# Firebase Web SDK Configuration (Frontend)
# Get these from Firebase Console → Project Settings → Web App
# If you don't have a Firebase project yet:
#   1. Go to https://console.firebase.google.com/
#   2. Create a new project or select existing one
#   3. Click the gear icon → Project settings
#   4. Scroll to "Your apps" → Add web app (</>) if needed
#   5. Copy the configuration values into these variables

VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=

# Firebase Analytics (OPTIONAL - only needed if using Google Analytics)
# Get this from Firebase Console → Project Settings → Web App → measurementId
VITE_FIREBASE_MEASUREMENT_ID=

# Example values (DO NOT USE IN PRODUCTION):
# VITE_FIREBASE_API_KEY=AIzaSyAaBbCcDdEeFfGgHhIiJjKkLlMmNnOoPpQq
# VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
# VITE_FIREBASE_PROJECT_ID=your-project-id
# VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
# VITE_FIREBASE_MESSAGING_SENDER_ID=123456789012
# VITE_FIREBASE_APP_ID=1:123456789012:web:abc123def456ghi789
# VITE_FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX

# ==================== #
# Optional: Backend Configuration
# ==================== #
# These variables are optional and only needed for advanced local development
# or backend/API server setup. Most developers won't need these.

# Backend Variables (optional for local development)
# PORT=3001
# NODE_ENV=development
# CORS_ORIGIN=http://localhost:5173
# VITE_API_URL=http://localhost:3001

# Backend Firebase Configuration (server-side only)
# NOTE: These unprefixed FIREBASE_* variables are for BACKEND/API server-side use ONLY
# FIREBASE_PROJECT_ID=
# FIREBASE_STORAGE_BUCKET=
# FIREBASE_SERVICE_ACCOUNT=

# ==================== #
# Vercel Deployment (CI/CD Only)
# ==================== #
# Required for automated deployments via GitHub Actions
# Configure these as GitHub Secrets in your repository settings

# VERCEL_TOKEN=           # Get from Vercel → Settings → Tokens
# VERCEL_ORG_ID=          # Get from Vercel → Settings → General
# VERCEL_PROJECT_ID=      # Get from Vercel → Project Settings → General

# ==================== #
# Google Cloud KMS (Optional - Advanced)
# ==================== #
# Only required if using server-side encryption features with Google Cloud KMS
# Most development doesn't require these

# GCP_PROJECT_ID=
# KMS_KEYRING_NAME=
# KMS_LOCATION=
# KMS_ENCRYPTION_KEY_NAME=
# KMS_KEY_VERSION=
# GOOGLE_APPLICATION_CREDENTIALS=
